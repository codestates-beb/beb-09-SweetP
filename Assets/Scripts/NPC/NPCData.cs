using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum NPCData
{
    Battle,
    Shop,
    Change
}

public enum BattleType
{
    Ground,
    Raid
}

public enum GroundMap
{
    Desert,
    Forest
}